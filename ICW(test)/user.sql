CREATE DATABASE user WITH ENCODING = 'UTF-8';

CREATE TABLE user (
	user_id  VARCHAR (6) PRIMARY KEY,
	id VARCHAR (16) NOT NULL ,
	pass  VARCHAR (16) dept(deptno)
	name VARCHAR (40)
	account_id VARCHAR (6)
) ;